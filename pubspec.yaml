name: cluck_scout
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4
  http: ^1.5.0
  http_parser: ^4.1.2
  path: ^1.9.1
  provider: ^6.1.5+1
  qr_flutter: ^4.1.0
  shared_preferences: ^2.5.3
  sqflite: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/background.png
    - assets/bird.png
    - assets/logo.PNG
    - assets/team_data/team_names.txt
    - assets/team_data/team_numbers.txt
    - assets/team_data/team_members.txt

